[workspace]
resolver = "2"
members = [
    "tmp-core",
    "tmp",
    "crates/command",
]
# crates/tmp-agent needs a sibling antigravity-sdk-rust checkout and is
# built separately. Cargo still fails workspace load if it stays in members.
