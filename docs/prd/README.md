# TMP PRDs

This directory contains product requirements documents for rebuilding and extending TMP.

Current PRD:

```text
docs/prd/tmp-rebuild-prd.md
```

Use it with the white paper:

```text
docs/whitepaper/tool-mapping-protocol.md
```

The white paper explains the protocol vision. The PRD turns that vision into an implementation plan that an AI agent or Rust engineer can execute phase by phase.
